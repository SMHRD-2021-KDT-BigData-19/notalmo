<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.board.mapper.TalmoTestMapper">
    <insert id="insert" parameterType="kr.board.entity.TalmoTestDTO">
        INSERT INTO result (selfcheck, frontpath, toppath)
        VALUES (#{selfcheck}, #{frontpath}, #{toppath})
    </insert>
</mapper>