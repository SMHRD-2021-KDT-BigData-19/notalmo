<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://mybatis.org/schema/mybatis-spring http://mybatis.org/schema/mybatis-spring.xsd
   http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd"
	xmlns:mybatis-spring="http://mybatis.org/schema/mybatis-spring">

	<!-- Root Context: defines shared resources visible to all other web components -->

	<!-- bean : 객체를 만드는 태그 -->
	<!-- 히카리CP환경설정(DB연결내용) -->
	<bean id="hikariConfig" class="com.zaxxer.hikari.HikariConfig">
		<property name="driverClassName" value="com.mysql.jdbc.Driver" />
		<property name="jdbcUrl" value="jdbc:mysql://project-db-campus.smhrd.com:3312/notalmo?useSSL=false&amp;serverTimezone=UTC" />
		<property name="username" value="notalmo" />
		<property name="password" value="notalmo" />
	</bean>

	<!-- hikariConfig 생성되면 사용할 수 있도록 HikariDataSource 객체 생성(커넥션 풀을 만든다) -->
	<bean id="dataSource" class="com.zaxxer.hikari.HikariDataSource"
		destroy-method="close">
		<constructor-arg ref="hikariConfig" />
	</bean>

	<!-- Mybatis가 커넥션풀과 연결, SQL문을 실행할 수 있는 SqlSessionFactoryBean -->
	<bean class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="dataSource" />
	</bean>

	<!-- mapper를 메모리에 올린다(scan) -->
	<mybatis-spring:scan base-package="kr.board.mapper" />

</beans>
   